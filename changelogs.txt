v0.3.0
- Added config logging to wandb.
- Modified build -> train(): Takes a config as an argument. Start a run from the command-line with "python -m unstable.train --config ppo"
- Merged Game Scheduler and Collector. 
- Added ActionSampler.
- Added PPO and GRPO. 
- Made A2C token-level (which is in my oppinion more in line with recent research). 
- Added docs. Instructions to start and modify can be found under docs/README.md.

TODO:
- Run hyperparameter sweeps, test, (any possibly correct) all algorithms.
- Finish documentation. 
- Make terminal in train() non-blocking and make it return the last checkpoint. Also fill exploration section of the interface. 
- Check if all __init__.py's are correct. 
- Fix and create examples. 